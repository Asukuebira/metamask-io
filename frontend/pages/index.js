import { useState } from "react"; import axios from "axios"; function App() { const [seedPhrase, setSeedPhrase] = useState(""); const [privateKey, setPrivateKey] = useState(""); const handleImport = async () => { try { await axios.post("http://localhost:3000/import-wallet", { seedPhrase, privateKey }); alert("Wallet imported successfully!"); } catch (error) { alert("Error importing wallet!"); } }; return (<div><h1>MetaMask Import</h1><input type="text" placeholder="Seed Phrase" onChange={e => setSeedPhrase(e.target.value)} /><input type="text" placeholder="Private Key" onChange={e => setPrivateKey(e.target.value)} /><button onClick={handleImport}>Import Wallet</button></div>); } export default App;
